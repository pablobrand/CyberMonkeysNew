import VideoSlider from "./VideoSlider";
import { SliderData } from "./SliderData";

import React, { useState } from "react";

const VideoSlider1 = (props) => {
    const [slide, setSlide] = useState(props.slide);
    
}
return 
export default VideoSlider1;